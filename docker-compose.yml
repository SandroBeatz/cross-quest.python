version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=False
      - PORT=5000
      - CORS_ORIGINS=*
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
