version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=False
      - PORT=8080
      - CORS_ORIGINS=*
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
